import 'package:flutter/widgets.dart';

class OrderConfirmation extends StatelessWidget {
  const OrderConfirmation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Order confirmation');
  }
}